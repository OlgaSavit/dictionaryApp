import { StyleSheet } from "react-native";

export const stylessheet = () =>
  StyleSheet.create({
    wrapperContent: {
      flex: 1,
      alignItems: "center",
      justifyContent: "center",
    },
  });
